Demo Project for Cypress E2E Testing Capabilities

Note that this project does not automatically run any build/test steps.

Get started by installing Node and running:
```
npm install
```

Run local tests via
```
npx cypress open
```

It's also recommended to run
```
npx eslint cypress/** --fix
```
before committing.
